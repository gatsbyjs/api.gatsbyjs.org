type Contributor {
  id: ID! @unique
  email: String @unique
  githubUsername: String! @unique
  shopifyCustomerID: String @unique
}

type Feedback {
  id: ID! @unique
  rating: Int!
  comment: String
  originUrl: String!
}
