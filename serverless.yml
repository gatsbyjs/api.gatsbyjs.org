service: serverless-graphql

plugins:
  - serverless-google-cloudfunctions
  - serverless-dotenv-plugin
  - serverless-webpack

custom:
  webpack:
    webpackConfig: ./webpack.config.js
    includeModules: true

provider:
  name: google
  stage: dev
  runtime: nodejs
  region: us-central1
  project: gatsby-core
  credentials: ~/Projects/keys/gatsby-core.json

functions:
  graphql:
    handler: graphql
    events:
      - http: foo
